# Task — 미해결 이슈 / 다음 작업

> 갱신: 2026-06-12

---

## 🔴 이슈 1: 📋 "컬럼+질문 프롬프트 복사" 버튼 — 동작 안 함

### 증상 (사용자 환경)
- 버튼을 눌러도 **완료 팝업이 안 뜸**
- **클립보드에 복사도 안 됨**
- 창이 뒤에 가려진 것 **아님** (Alt+Tab으로 확인 완료)
- 누르면 앱이 멈추는 것처럼 보임

### 시도한 수정 (효과 없음)
- `messagebox`에 `parent=self.root` 지정 (창 가림 가설 → 기각됨)

### 유력 가설 (다음 세션에서 검증)
1. **사내 보안/DLP 프로그램의 클립보드 차단** ← 최우선 의심
   - 회사 PC의 DLP가 프로그램의 클립보드 접근(`clipboard_clear/append`)을 차단·지연시키면
     해당 호출에서 **행(hang)** → 팝업 단계까지 도달 못 함 → "멈춤"과 정확히 일치
2. 콜백 내부 예외 (Tkinter는 콜백 예외를 stderr에만 출력하고 조용히 무시)

### 디버깅/수정 계획
- [ ] 사용자 PC에서 콘솔 실행으로 stderr 캡처: `python auto_synthesize_gui_dpv2.py 2> err.log`
- [ ] `_copy_columns_csv`에 try/except + 로그 추가하여 어느 줄에서 멈추는지 특정
- [ ] **클립보드 우회 fallback 구현**:
  - 방법 A: Tk 클립보드 대신 Windows `clip.exe` 파이프 사용 (`subprocess`)
  - 방법 B (보안 차단에도 안전): 프롬프트를 **선택 가능한 Text 창**에 띄우고
    사용자가 `Ctrl+A` → `Ctrl+C`로 직접 복사 (자동 복사 실패 시 자동 fallback)
- [ ] 클립보드 호출에 타임아웃/예외 처리 추가 — 실패해도 앱이 멈추지 않게

---

## 🔴 이슈 2: 📥 "AI 답변 설명 일괄 붙여넣기" — 빈 창이 뜸

### 증상 (사용자 환경)
- 대화상자(Toplevel)가 열리긴 하나 **내용이 아무것도 안 보임** (안내문/입력창/버튼 없음)

### 시도한 수정 (효과 미확인/부족)
- 메인 창 중앙 강제 배치 + 화면 클램프
- `topmost` 잠깐 켰다 끄기, `grab_set`을 창 가시화 후로 지연, Esc 닫기 추가

### 유력 가설
1. **위젯 빌드 도중 예외** — Toplevel 생성 직후 예외가 나면 빈 창만 남음
   (Tkinter 콜백 예외는 화면에 안 보이고 stderr로만 감)
2. 특정 환경(테마/DPI 배율)에서 `ttk` 위젯 렌더 문제
3. geometry/topmost 호출 순서가 해당 환경에서 레이아웃을 깨뜨림

### 디버깅/수정 계획
- [ ] stderr 캡처로 예외 여부 확인 (이슈 1과 동일 방법)
- [ ] 위젯 빌드 전체를 try/except로 감싸고 실패 시 에러 내용을 대화상자에 표시
- [ ] 위젯을 **모두 빌드한 뒤** geometry/topmost/grab 적용하도록 순서 변경
- [ ] 환경 차이 기록: 발생 PC (사용자 13인치 노트북) vs 미발생 PC 여부 비교

---

## 환경 메모

| 항목 | 값 |
|---|---|
| 발생 환경 | 사용자 PC (13인치급, 1536×864, 작업영역 높이 793px, Windows) |
| 개발 환경 | 동일 해상도 — 단, 버튼 동작 여부 직접 재현 확인은 미완 |
| 의심 요소 | 사내 보안 솔루션(DLP)의 클립보드 정책, DPI 배율 150% |

---

## 🟡 그 외 대기 중 작업

- [ ] **13인치 화면 검증**: ③ 관계 확정 버튼/④ 파일 확정 버튼이 좁은 화면에서 보이는지 최종 확인
  (세로<850px 시 로고 생략·패딩 축소·로그 3줄 적용함 — 사용자 확인 대기)
- [ ] **send_mail.py 설정**: `.env`에 `GMAIL_USER`/`GMAIL_APP_PW`(앱 비밀번호) 설정 후 발송 테스트.
  내부망에서 `smtp.gmail.com:465` 아웃바운드 차단 여부 확인 필요
- [ ] **세션 간 공유 매핑 재사용**(아이디어): 관계 있는 테이블을 따로 올려도 이전 변환키를
  불러와 같은 가짜값을 유지하는 기능 — 관계 보존 문제 해결용
- [ ] **PyInstaller exe 빌드**: `pyinstaller --onefile --windowed` (리소스 추가 시 `--add-data`)
